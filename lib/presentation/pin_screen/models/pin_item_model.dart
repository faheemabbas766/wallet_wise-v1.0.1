import '../../../core/app_export.dart';/// This class is used in the [pin_item_widget] screen.
class PinItemModel {PinItemModel({this.one, this.id, }) { one = one  ?? "1";id = id  ?? ""; }

String? one;

String? id;

 }
