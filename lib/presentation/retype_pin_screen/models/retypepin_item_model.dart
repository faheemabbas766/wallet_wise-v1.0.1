import '../../../core/app_export.dart';/// This class is used in the [retypepin_item_widget] screen.
class RetypepinItemModel {RetypepinItemModel({this.one, this.id, }) { one = one  ?? "1";id = id  ?? ""; }

String? one;

String? id;

 }
